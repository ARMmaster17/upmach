<%= form_for([@machine, @downtime]) do |form| %>
  <% if downtime.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(downtime.errors.count, "error") %> prohibited this downtime from being saved:</h2>

      <ul>
      <% downtime.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :start_time %>
    <%= form.datetime_select :start_time, id: :downtime_start_time %>
  </div>

  <div class="field">
    <%= form.label :end_time %>
    <%= form.datetime_select :end_time, id: :downtime_end_time %>
  </div>

  <div class="field">
    <%= form.label :machine_id %>
    <%= form.text_field :machine_id, id: :downtime_machine_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
